
const users = {
  address: "Av. Aca 1122",
  name: "Nathan"
}

let parLlaveValor = Object.entries(users);
console.log(parLlaveValor);

let soloPropiedades = Object.keys(users)
console.log(soloPropiedades);

let soloValues = Object.values(users)
console.log(soloValues);
